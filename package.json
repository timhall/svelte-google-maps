{
  "name": "svelte-google-maps-monorepo",
  "private": true,
  "workspaces": [
    "packages/*",
    "docs"
  ],
  "scripts": {
    "build": "cd packages/svelte-google-maps && yarn build && cd ../../docs && yarn build"
  },
  "prettier": {
    "singleQuote": true,
    "printWidth": 100
  }
}
